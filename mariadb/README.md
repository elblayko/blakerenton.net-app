Create `.env` and configure the following environment variables as required.

.env:
```
MARIADB_ROOT_PASSWORD=my-password
MARIADB_DATABASE=wordpress
MARIADB_USER=wordpress
MARIADB_PASSWORD=my-password
```